<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.observer">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Fire_Effect_Activity"></activity>
        <activity android:name=".Confirm_Fire_Activity"></activity>
        <activity android:name=".Target_Data_Activity"></activity>
        <activity android:name=".Observer_Home_Activity" ></activity>
        <activity android:name=".Log_in_Activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>